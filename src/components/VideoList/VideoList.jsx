

const VideoList = (props) => {
   
    

    return (
        <>
            {/* <div>
            {props.list.map(video => {
                <div key={video.id}>
                    <button>{video.title}</button>
                </div>
            })}
            
            </div> */}

        </>
    )
}

export default VideoList;